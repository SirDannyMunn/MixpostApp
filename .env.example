# This value is the name of your application.
APP_NAME='Mixpost'

# Key used to encrypt and decrypt sensitive data. Generate this using the following tool:
# https://mixpost.app/tools/encryption-key-generator
APP_KEY=

# Debug mode setting. Set to 'false' for production environments.
APP_DEBUG=false

# Full application URL. Important: it is not allowed to be a slash at the end of the url.
APP_URL=https://example.com

# Optional: token-based auth for Ploi Horizon panel integration.
# If set, Ploi can access Horizon using either:
# - Authorization: Bearer <token>
# - X-Horizon-Token: <token>
# - /horizon?token=<token>
HORIZON_PLOI_TOKEN=

# CORS (set these in dev to allow local/frontends like Figma Preview)
# Comma-separated exact origins. Example:
# CORS_ALLOWED_ORIGINS=https://f16099a3-f99f-4a71-81f2-1c974ee072a9-v2-figmaiframepreview.figma.site
# Pipe-separated regex patterns for wildcard origins. Example:
# CORS_ALLOWED_ORIGINS_PATTERNS=^https://([a-z0-9-]+\.)*figma\.site$
# Send Access-Control-Allow-Credentials: true if your frontend uses cookies
# CORS_SUPPORTS_CREDENTIALS=false

# Sanctum (only if using cookie-based SPA auth)
# SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1

# MySQL connection setup
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=mixpost_db
DB_USERNAME=root
DB_PASSWORD=

# Redis connection setup
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Optional default proxy URL for Rebrowser runs used by PhantomBrowse sessions.
# Format: protocol://user:pass@host:port
PHANTOMBROWSE_PROXY_URL=

# The disk on which to store added files. Disks: "public", "s3". For "s3" you should fill in the AWS_* credentials below.
MIXPOST_DISK=public

# AWS Credentials
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=
AWS_BUCKET=

# SMTP (Resend)
MAIL_MAILER=resend
MAIL_FROM_ADDRESS="leads@velocity.dev"
MAIL_FROM_NAME="${APP_NAME}"
RESEND_API_KEY=

# Evaluation harness defaults
AI_EVAL_SCOPE_TO_KI=true
AI_EVAL_GRADER_MODE=contains
